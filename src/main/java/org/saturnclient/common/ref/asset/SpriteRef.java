package org.saturnclient.common.ref.asset;

import org.saturnclient.common.MinecraftBinding;

public class SpriteRef extends MinecraftBinding {
    public SpriteRef(Object binding) {
        super(binding);
    }
}
